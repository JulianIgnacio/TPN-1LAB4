b = float(input("Ingrese el valor de la base del triangulo que deseas calcular: "))
h = float(input("Ingrese el valor de la altura del triangulo que deseas calcular: "))
area = b*h // 2
print(f"El área del triangulo es: {area}")